import {expect} from 'chai';

describe('Get Routes', () => {
    let x = 0;

    beforeEach(() => {
        x++;
    });

    it('should do something', () => {
        expect(1).to.equal(x);
    });

    it('should do something', () => {
        expect(2).to.equal(x);
    });
});